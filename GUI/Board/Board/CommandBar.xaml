<UserControl x:Class="Board.CommandBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500">
    <StackPanel Orientation="Horizontal">
        <StackPanel.Resources>
            <Style TargetType="{x:Type Button}">
                <Setter Property="Margin" Value="3"></Setter>
                <Setter Property="Padding" Value="5"></Setter>
            </Style>
        </StackPanel.Resources>
		<Button Command="{Binding ClickCommand}" CommandParameter="spawn">Spawn</Button>
		<Button Command="{Binding ClickCommand}" CommandParameter="moveE">Move E</Button>
		<Button Command="{Binding ClickCommand}" CommandParameter="moveW">Move W</Button>
		<Button Command="{Binding ClickCommand}" CommandParameter="moveSE">Move SE</Button>
		<Button Command="{Binding ClickCommand}" CommandParameter="moveSW">Move SW</Button>
		<Button Command="{Binding ClickCommand}" CommandParameter="rotateClock">Turn clock</Button>
		<Button Command="{Binding ClickCommand}" CommandParameter="rotateCounterClock">Turn counter-clock</Button>
	</StackPanel>
</UserControl>
