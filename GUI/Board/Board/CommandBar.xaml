<UserControl x:Class="Board.CommandBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500">
    <StackPanel Orientation="Horizontal">
        <StackPanel.Resources>
            <Style TargetType="{x:Type Button}">
                <Setter Property="Margin" Value="3"></Setter>
                <Setter Property="Padding" Value="5"></Setter>
            </Style>
        </StackPanel.Resources>
		<Button Command="{Binding ClickCommand}" CommandParameter="spawn">Spawn</Button>
        <UniformGrid>
            <Button Command="{Binding ClickCommand}" CommandParameter="West">Move W</Button>
            <Button Command="{Binding ClickCommand}" CommandParameter="East">Move E</Button>
            <Button Command="{Binding ClickCommand}" CommandParameter="SouthWest">Move SW</Button>
            <Button Command="{Binding ClickCommand}" CommandParameter="SouthEast">Move SE</Button>
        </UniformGrid>
        <Button Command="{Binding ClickCommand}" CommandParameter="RotateClockwise">Rotate clock</Button>
        <Button Command="{Binding ClickCommand}" CommandParameter="RotateCounterClockwise">Rotate counter-clock</Button>
		<Button Command="{Binding ClickCommand}" CommandParameter="StartSolver" x:Name="btnSolverStart">Start Solver</Button>
		<Button Command="{Binding ClickCommand}" CommandParameter="NextSolverStep" x:Name="btnNextSolve" IsEnabled="False">Next solve</Button>
		<Button Command="{Binding ClickCommand}" CommandParameter="ShowInSnapshot" x:Name="btnShowIn" IsEnabled="False">Show in snapshot</Button>
		<Button Command="{Binding ClickCommand}" CommandParameter="ShowOutSnapshot" x:Name="btnShowOut" IsEnabled="False">Show out snapshot</Button>
		<Button Command="{Binding ClickCommand}" CommandParameter="NextMoveStep" x:Name="btnNextMove" IsEnabled="False">Next move</Button>
	</StackPanel>
</UserControl>
